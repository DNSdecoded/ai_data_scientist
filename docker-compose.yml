version: "3.8"

services:
  ai-data-scientist:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./experiments:/app/experiments
      - ./outputs:/app/outputs
      - ./logs:/app/logs
    restart: unless-stopped
